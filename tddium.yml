:tddium:
  :hooks:
    :pre:  'wget http://pear.php.net/go-pear.phar && php go-pear.phar && pear channel-discover pear.phing.info && pear install phing/phing && phpenv rehash && composer install'
  :tests:
    - phing test
